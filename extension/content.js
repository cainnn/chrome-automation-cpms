// Content script placeholder — DOM operations are injected via chrome.scripting.executeScript
// from the background service worker for better reliability across page types.

console.debug('[AutomationBridge] Content script loaded on', location.href);
